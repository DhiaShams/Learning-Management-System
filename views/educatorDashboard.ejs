<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Educator Dashboard</title>
  <meta name="csrf-token" content="<%= csrfToken %>" />
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-gray-50 font-sans">

  <nav class="bg-purple-700 text-white px-6 py-4 flex justify-between items-center">
    <h1 class="text-xl font-bold">LMS Educator Dashboard</h1>
    <form action="/logout" method="POST" class="bg-white text-blue-600 px-4 py-2 rounded hover:bg-gray-200 transition">
      <input type="hidden" name="_csrf" value="<%= csrfToken %>">
      <button type="submit">Logout</button>
    </form>
  </nav>

  <main class="max-w-6xl mx-auto mt-8 p-4">
    <h2 class="text-2xl font-semibold mb-6">Welcome, <%= educatorName %> 👋</h2>

    <section class="mb-12">
      <div class="flex justify-between items-center mb-4">
        <h3 class="text-xl font-bold">Your Courses</h3>
        <a href="/educator/course/new" class="bg-purple-600 text-white px-4 py-2 rounded hover:bg-purple-500 transition">Add New Course</a>
      </div>

      <% if (courses && courses.length === 0) { %>
        <p class="text-gray-600">You haven’t created any courses yet.</p>
      <% } else { %>
        <div class="grid md:grid-cols-2 gap-6">
          <% courses.forEach(course => { %>
            <div class="bg-white shadow-md rounded-2xl p-6">
              <h4 class="text-lg font-semibold mb-1"><%= course.title %></h4>
              <p class="text-gray-600 mb-3"><%= course.description %></p>
              <div class="flex justify-between text-sm">
                <a href="/educator/courses/<%= course.id %>/edit" class="text-blue-600 hover:underline">Edit</a>
                <a href="/educator/courses/<%= course.id %>/lessons" class="text-green-600 hover:underline">Manage Lessons</a>
              </div>
            </div>
          <% }); %>
        </div>
      <% } %>
    </section>

    <section>
      <h3 class="text-xl font-bold mb-4">Student Feedback</h3>
      <% if (reviews && reviews.length === 0) { %>
        <p class="text-gray-600">No feedback received yet.</p>
      <% } else { %>
        <ul class="space-y-4">
          <% reviews.forEach(review => { %>
            <li class="bg-white p-4 rounded-lg shadow-md">
              <p class="font-medium"><%= review.studentName %> - <span class="text-yellow-500">★ <%= review.rating %></span></p>
              <p class="text-gray-700 mt-2"><%= review.comment %></p>
            </li>
          <% }); %>
        </ul>
      <% } %>
    </section>
  </main>

</body>
</html>
