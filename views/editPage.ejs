<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit Page - <%= page.title %></title>
  <meta name="csrf-token" content="<%= csrfToken %>" />
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-gray-50 font-sans">
  <nav class="bg-purple-700 text-white px-6 py-4 flex justify-between items-center">
    <h1 class="text-xl font-bold">Edit Page for <%= page.lesson.title %></h1>
    <a href="/educator/courses/<%= page.lesson.course.id %>/lessons" class="bg-white text-purple-700 px-4 py-2 rounded hover:bg-gray-100 transition">Back to Lessons</a>
  </nav>

  <main class="max-w-4xl mx-auto mt-8 p-4 bg-white shadow rounded">
    <h2 class="text-2xl font-semibold mb-6">Edit Page</h2>
    <form method="POST" action="/educator/pages/<%= page.id %>/edit" class="space-y-4">
      <input type="hidden" name="_csrf" value="<%= csrfToken %>">
      <div>
        <label class="block text-sm font-medium text-gray-700">Page Title</label>
        <input type="text" name="title" value="<%= page.title %>" required class="mt-1 block w-full border border-gray-300 rounded-md shadow-sm p-2">
      </div>
      <div>
        <label class="block text-sm font-medium text-gray-700">Page Content</label>
        <textarea name="content" required class="mt-1 block w-full border border-gray-300 rounded-md shadow-sm p-2"><%= page.content %></textarea>
      </div>
      <button type="submit" class="bg-purple-600 text-white px-4 py-2 rounded hover:bg-purple-500 transition">Save Changes</button>
    </form>
  </main>
</body>
</html>